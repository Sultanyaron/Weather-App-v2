import React from 'react';
import sunSpinner from '../../../assets/images/sunLoading.png';

const Spinner = () => {
  return <img className="spinner" src={sunSpinner} alt="Loading..." />;
};

export default Spinner;